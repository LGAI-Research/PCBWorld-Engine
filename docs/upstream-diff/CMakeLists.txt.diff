# GENERATED by tools/make_upstream_diff.sh — for reading only, not a build input; see README.md in this directory.
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -864,22 +864,21 @@
 include( MinGWResourceCompiler )
 
 # Find ngspice library, required for integrated circuit simulator
-find_package( ngspice REQUIRED )
+# Made optional for headless RL router build (ngspice shared lib not needed)
+find_package( ngspice )
 
 # Find OpenCascade, required for STEP plugin and tools
+# Made optional for headless RL router build
 find_package(OCC)
-if( NOT OCC_FOUND )
-    MESSAGE( FATAL_ERROR "================================================================\n"
-                         " OpenCASCADE was not found!\n"
-                         "================================================================\n")
+if( OCC_FOUND )
+    if( OCC_VERSION_STRING VERSION_LESS 7.5.0 )
+        MESSAGE( WARNING "OpenCASCADE version ${OCC_VERSION_STRING} found, minimum 7.5.0 required.")
+        set( OCC_FOUND FALSE )
+    else()
+        include_directories( SYSTEM ${OCC_INCLUDE_DIR} )
+        add_compile_definitions( KICAD_USE_OCC )
+    endif()
 endif()
-if( OCC_VERSION_STRING VERSION_LESS 7.5.0 )
-    MESSAGE( FATAL_ERROR "================================================================\n"
-                         "OpenCASCADE version ${OCC_VERSION_STRING} was found.\n"
-                         "  KiCad requires a minimum version of 7.5.0\n"
-                         "================================================================\n")
-endif()
-include_directories( SYSTEM ${OCC_INCLUDE_DIR} )
 
 if( KICAD_USE_CMAKE_FINDPROTOBUF )
     include( FindProtobuf )
