# GENERATED by tools/make_upstream_diff.sh — for reading only, not a build input; see README.md in this directory.
--- a/kicad/cli/command_pcb_render.cpp
+++ b/kicad/cli/command_pcb_render.cpp
@@ -334,7 +334,7 @@
 
     renderJob->m_appearancePreset = m_argParser.get<std::string>( ARG_PRESET );
 
-    if( renderJob->m_appearancePreset == LEGACY_PRESET_FLAG )
+    if( renderJob->m_appearancePreset == std::string( wxString( LEGACY_PRESET_FLAG ).ToUTF8() ) )
     {
         wxFprintf( stderr, _( "Invalid preset\n" ) );
         return EXIT_CODES::ERR_ARGS;
