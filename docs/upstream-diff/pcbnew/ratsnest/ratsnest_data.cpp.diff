# GENERATED by tools/make_upstream_diff.sh — for reading only, not a build input; see README.md in this directory.
--- a/pcbnew/ratsnest/ratsnest_data.cpp
+++ b/pcbnew/ratsnest/ratsnest_data.cpp
@@ -209,21 +209,14 @@
         }
         else if( anchors.size() == 1 )
         {
-            // The anchors all have the same position, but may not have overlapping layers.
-            prev = nullptr;
-
-            for( const std::shared_ptr<CN_ANCHOR>& n : m_allNodes )
-            {
-                if( prev && !( prev->Parent()->GetLayerSet() & n->Parent()->GetLayerSet() ).any() )
-                {
-                    // Use a minimal but non-zero distance or the edge will be ignored
-                    mstEdges.emplace_back( prev, n, 1 );
-                }
-
-                prev = n;
-            }
-
-            return;
+            // All anchors share one position — nothing to triangulate. Fall
+            // through to the per-position anchor-chain pass below, which emits
+            // one weight-1 edge per pair of DISTINCT clusters (cluster
+            // identity). A Parent()->GetLayerSet() overlap test over
+            // consecutive nodes would not do: parents report layer sets wider
+            // than their actual copper (through vias/pads, multi-layer zones),
+            // so a cross-cluster pair could be silently treated as connected
+            // and the required edge dropped (undercount).
         }
         else if( areNodesColinear( anchors ) )
         {
