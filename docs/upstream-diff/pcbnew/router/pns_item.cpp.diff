# GENERATED by tools/make_upstream_diff.sh — for reading only, not a build input; see README.md in this directory.
--- a/pcbnew/router/pns_item.cpp
+++ b/pcbnew/router/pns_item.cpp
@@ -24,6 +24,7 @@
 #include "pns_line.h"
 #include "pns_router.h"
 
+#include <board_item.h>
 #include <geometry/shape_compound.h>
 #include <geometry/shape_poly_set.h>
 
@@ -91,6 +92,21 @@
 }
 
 
+void ITEM::SetParent( BOARD_ITEM* aParent )
+{
+    m_parent = aParent;
+
+    // Cache the parent's UUID alongside the pointer. canonicalItemCompare() reads the
+    // cached copy so no ordering ever has to dereference m_parent — NODE::Commit sorts
+    // items whose board parents the iface has already deleted, and that is exactly the
+    // use-after-free that used to force a second, UUID-less comparator.
+    m_parentUuid = aParent ? aParent->m_Uuid : KIID( 0 );
+
+    if( m_parent )
+        m_sourceItem = m_parent;
+}
+
+
 std::set<int> ITEM::RelevantShapeLayers( const ITEM* aOther ) const
 {
     std::vector<int> myLayers = UniqueShapeLayers();
@@ -259,6 +275,8 @@
                     collisionsFound = true;
                     OBSTACLE obs;
                     obs.m_head = const_cast<ITEM*>( aHead );
+                    obs.m_headSerial = aHead->Serial();   // by-value ordering key (head may be a
+                                                          // loop-scoped temporary; see OBSTACLE)
                     obs.m_item = const_cast<ITEM*>( this );
                     obs.m_clearance = clearance;
                     obs.m_distFirst = 0;
@@ -284,6 +302,8 @@
                     collisionsFound = true;
                     OBSTACLE obs;
                     obs.m_head = const_cast<ITEM*>( aHead );
+                    obs.m_headSerial = aHead->Serial();   // by-value ordering key (head may be a
+                                                          // loop-scoped temporary; see OBSTACLE)
                     obs.m_item = const_cast<ITEM*>( this );
                     obs.m_clearance = clearance;
                     obs.m_distFirst = 0;
