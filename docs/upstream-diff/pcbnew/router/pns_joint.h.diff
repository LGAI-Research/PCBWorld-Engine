# GENERATED by tools/make_upstream_diff.sh — for reading only, not a build input; see README.md in this directory.
--- a/pcbnew/router/pns_joint.h
+++ b/pcbnew/router/pns_joint.h
@@ -324,7 +324,12 @@
 
     bool operator==( const JOINT& rhs )  const
     {
-        return m_tag.pos == rhs.m_tag.pos && m_tag.net == rhs.m_tag.net;
+        // Exact identity: position, net AND layer range. Comparing pos+net
+        // alone would conflate two stacked joints at the same (x, y) on
+        // different layers (F/B track ends without a via). Overlaps() below
+        // is the interval predicate; this is exact identity.
+        return m_tag.pos == rhs.m_tag.pos && m_tag.net == rhs.m_tag.net
+                && m_layers == rhs.m_layers;
     }
 
     void Merge( const JOINT& aJoint )
