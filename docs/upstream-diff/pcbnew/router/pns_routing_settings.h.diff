# GENERATED by tools/make_upstream_diff.sh — for reading only, not a build input; see README.md in this directory.
--- a/pcbnew/router/pns_routing_settings.h
+++ b/pcbnew/router/pns_routing_settings.h
@@ -131,6 +131,14 @@
     int ShoveIterationLimit() const;
     TIME_LIMIT ShoveTimeLimit() const;
 
+    // RL determinism: the shove loop and TOPOLOGY::followBranch are bounded by
+    // these iteration counts rather than by a wallclock timeout, so a run is
+    // reproducible. Exposed so the RL engine can configure them at init like
+    // engine_seed; the defaults are 250 / 1,000,000.
+    void SetShoveIterationLimit( int aLimit ) { m_shoveIterationLimit = aLimit; }
+    int FollowBranchIterLimit() const { return m_followBranchIterLimit; }
+    void SetFollowBranchIterLimit( int aLimit ) { m_followBranchIterLimit = aLimit; }
+
     int WalkaroundIterationLimit() const { return m_walkaroundIterationLimit; };
     TIME_LIMIT WalkaroundTimeLimit() const;
 
@@ -181,6 +189,7 @@
 
     int m_walkaroundIterationLimit;
     int m_shoveIterationLimit;
+    int m_followBranchIterLimit = 1000000;   // Determinism: TOPOLOGY::followBranch DFS pop cap
     int m_viaForcePropIterationLimit;
     double m_walkaroundHugLengthThreshold;
 
