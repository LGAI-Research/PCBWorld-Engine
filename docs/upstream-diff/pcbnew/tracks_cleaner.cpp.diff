# GENERATED by tools/make_upstream_diff.sh — for reading only, not a build input; see README.md in this directory.
--- a/pcbnew/tracks_cleaner.cpp
+++ b/pcbnew/tracks_cleaner.cpp
@@ -564,6 +564,16 @@
                         if( candidate < segment ) // avoid duplicate merges
                             continue;
 
+                        // Candidates arrive via connectivity, which crosses
+                        // layers through vias/thru-pads, so only same-layer
+                        // segments may merge: two collinear same-net
+                        // same-width tracks on DIFFERENT layers joined by a
+                        // via would otherwise pass every check and
+                        // mergeCollinearSegments would silently delete the
+                        // other layer's segment.
+                        if( candidate->GetLayer() != segment->GetLayer() )
+                            continue;
+
                         if( segment->ApproxCollinear( *candidate )
                             && testMergeCollinearSegments( segment, candidate ) )
                         {
